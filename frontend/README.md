# Frontend Code

This directory contains the two frontend surfaces that share the same backend APIs.

- `frontend/mobile/`: Existing Expo/React Native mobile frontend code.
- `frontend/web/`: Stage 1 PHP web-app extension code.

Both frontends are expected to consume backend routes under `backend/api`.
