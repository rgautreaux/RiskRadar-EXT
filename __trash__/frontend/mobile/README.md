# Mobile Frontend

The existing RiskRadar mobile application lives under `frontend/mobile/RiskRadar/`.

## Stack

- Expo
- React Native

## Notes

This mobile frontend remains separate from the CMPS 357 web-app extension so the new web work can proceed independently while both clients continue using the same backend services in `backend/`.

For the current app starter instructions, see `frontend/mobile/RiskRadar/README.md`.
